---
original: "A 'yield' expression is only allowed in a generator body."
---

The \`yield\` keyword can only be used inside a generator function

---zh---

\`yield\` 关键字只能在生成器函数（generator function，即带 \`*\` 的函数）内部使用。
