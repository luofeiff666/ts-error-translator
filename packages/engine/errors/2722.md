---
original: "Cannot invoke an object which is possibly 'undefined'."
---

This function might be undefined. You'll need to check it's defined before calling it.

---zh---

这个函数可能是 \`undefined\`，调用前需要先判断它是否存在，例如使用可选链：\`obj.fn?.()\`。
