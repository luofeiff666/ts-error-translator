---
original: 'A rest parameter must be last in a parameter list.'
---

A parameter in a function that starts with \`...\` must be the last one in the list.

---zh---

以 \`...\` 开头的剩余参数（rest parameter）必须放在参数列表的最后一位。
